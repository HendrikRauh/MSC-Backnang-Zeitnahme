extends /base.pug

append variables
    - baseTitle = "Datenbank"

append styles
    link(rel="stylesheet" href="/css/database.css")

append scripts
    script(src="/js/database.js") 

append content
    h1 Datenbank wird geladen...
        p#dbPort #{dbPort}
    iframe(id="database")