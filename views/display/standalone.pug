extends /base.pug

append variables
    - baseTitle = "Display - Standalone"

append scripts 
    script(src="/js/standalone.js")

append styles
    link(rel="stylesheet" href="/css/standalone.css")

append content
    if lastRun != ""
        #last-run #{lastRun}

    if portOpened == null
        #error 
            p ❌ LICHTSCHRANKE ❌
            button#reconnect NEU VERBINDEN
    else 
        if running 
            #running 🔴 MESSUNG 🔴
        else
            #running 🟢 BEREIT 🟢

